- name: Generate config.js
        run: |
          python3 -c "
          import sys
          text = open('config.template.js').read()
          text = text.replace('%%MY_GITHUB_TOKEN%%', '${{ secrets.MY_GITHUB_TOKEN }}')
          open('config.js', 'w').write(text)
          "
          echo "✅ config.js generated"